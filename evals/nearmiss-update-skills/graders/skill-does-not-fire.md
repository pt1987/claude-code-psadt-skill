---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"(?:[\w-]+:)?psadt-deploy"'
min: 0
max: 0
arm: both
---

THE important one. Until this release the description carried "update skill" as a trigger phrase with no namespace, so this skill answered for every other updatable skill installed on the machine. "psadt update" stays; the bare phrase is gone.
